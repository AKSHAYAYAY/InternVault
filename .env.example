# .env.example

# Set a dummy PostgreSQL connection string or use a local one
DATABASE_URL="postgresql://postgres:password@localhost:5432/internvault?schema=public"

# Auth.js secret (can be generated using `npx auth secret`)
AUTH_SECRET="your-super-secret-auth-key-change-in-production"
